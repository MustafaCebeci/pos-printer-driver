[package]
name = "pos-printer-protocol"
version.workspace = true
edition.workspace = true

[dependencies]
thiserror.workspace = true
log.workspace = true
image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }
